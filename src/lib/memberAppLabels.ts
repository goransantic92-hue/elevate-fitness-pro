/** Navbar / CTAs: visible label for routes under `/dashboard` (purchased program app). */
export const MEMBER_APP_LINK_LABEL = "Your Training";

/** Buttons when user already has access (sentence case). */
export const MEMBER_APP_OPEN_CTA = "Open your training";
